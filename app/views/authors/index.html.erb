<% @authors.each do |author| %>
    <%= link_to author.name + " " + author.surname, author_path(author) %>
    <%= image_tag(author.image.url(:medium), {:alt => 'author_photo_' + author.id.to_s }) %>
    <%= author.country %>
<% end %>
